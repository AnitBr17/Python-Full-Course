name = "Harry is a good  boy and "


print(name.replace("  ", " "))

# The replace() method is used to replace a specified phrase with another specified phrase.
# Here, we are replacing double spaces with a single space.

print(name) # Strings are immutable which means that you cannot change them by running functions on them
